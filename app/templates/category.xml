<?xml version="1.0" encoding="UTF-8"?>
<v>
    {% for post in posts %}
    <article num="{{ loop.index }}">
        <title>
        {{ post.title }}
        </title>
        <content>
        {{ post.content|truncate(200, True)|striptags}}
        </content>
        <time>
        {{ post.time }}
        </time>
        <author>
        {{ post.author.username }}
        </author>
        <cate>
        {{ post.cat.name }}
        </cate>
        <url>
        {{ url_for('article_detail', username=post.author.username, post_id=post.id)}}
        </url>
    </article>
    {% endfor %}
</v>